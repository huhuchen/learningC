#include <stdlib.h>
#include <stdio.h>

int strcmp(char *line1, char *line2);
int readlines(char *line[], int max);
void writelines(char *line[], int limit);
void swap(char *line[], int left, int right);
